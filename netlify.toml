[build]
  command = "npm run build"
  publish = "dist/poke-finder/browser"

# Client-side routing fallback - same idea as the 404.html trick for
# GitHub Pages, just expressed the way Netlify wants it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
